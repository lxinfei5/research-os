# Architecture

## Product cores

**First principle:** lowest cognitive load to *act* (`AGENTS.md` §0).  
Five innovations under `pillars/` are **means**. Skills execute; topics store instances; tools adapt.

```
          ┌──────────────────────────────────────────┐
          │  First principle: lowest burden to act   │
          │           AGENTS.md constitution         │
          └──────────────────┬───────────────────────┘
                             │
     ┌───────────┬───────────┼───────────┬───────────┐
     ▼           ▼           ▼           ▼           ▼
 half-life  corroboration discovery  thinking    output
  L0–L3      2-of-N    user-trusted logical    user
  (don't     (don't     (don't      (don't     surface
   babysit    weigh      hunt;       extract    first;
   stale)     20 links)  empty loud) tradeoff)  audit back
     │           │           │           │           │
     └───────────┴─────┬─────┴───────────┴───────────┘
                       ▼
              researchos-grow skill
                       │
          ┌────────────┴────────────┐
          ▼                         ▼
   user surface              topics/*/knowledge.md
   (act / hold / flip)       (audit / L0–L3)
```

## Directory rules

See `pillars/README.md` and `AGENTS.md` §1.
