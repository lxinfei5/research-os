"""Boundary gates — mechanical invariants that keep the engine honest. Run by `ros lint` (and the
Stop hook). Rules are documented in anti_corruption.md; each gate is a pure check returning problems."""
