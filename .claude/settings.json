{
  "//": "ResearchOS project settings. The Stop hook runs the boundary gates (ros lint) after each turn so corruption/regressions surface immediately. The SessionEnd hook reaps rod-Chrome orphans left by headless xiaohongshu-mcp anti-bot EOF panics (each holds the user's XHS login cookies in memory). Remove or set to '|| true' if you prefer non-blocking.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/tools/hooks/run-boundary-lint.sh\""
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/tools/hooks/cleanup-social-mcp.sh\""
          }
        ]
      }
    ]
  }
}
